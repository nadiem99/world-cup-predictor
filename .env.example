# Copy this file to .env and fill in your key.
# Get an OpenRouter API key at https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-...

# Optional: sampling temperature for predictions.
# Lower = the model's single best guess (recommended for a prediction contest).
PREDICT_TEMPERATURE=0.2
